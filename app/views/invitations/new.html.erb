<div class="w-full px-6 pb-6 mx-auto md:w-2/3">
  <h1 class="text-4xl font-bold text-white font-nimbus-ext">Nueva invitación</h1>
  <span class="px-1 mb-2 -mt-6 text-sm font-semibold tracking-wide text-gray-600 uppercase w-fit font-nimbus-ext"><%= @cadena.name %></span>
  <%= render "form", invitation: @invitation %>
  <%= link_to 'Ver invitaciones', cadena_invitations_path, class: "ml-2 rounded-xl p-4 bg-gray-100 inline-block font-medium" %>
</div>
