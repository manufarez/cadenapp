<h1 class="mt-6 mb-2 text-lg font-semibold"><%= current_user == @user ? 'Mis pagos' : 'Pagos' %></h1>
<div class="flex flex-col space-y-4">
  <% if @user.made_payments.any? %>
    <% @user.made_payments.each do |payment| %>
      <div class="flex items-center justify-between p-4 font-sans text-sm font-medium rounded-xl bg-midnight hover:bg-black_rock">
        <div class="flex items-center justify-between w-full">
          <div class="flex flex-col">
            <div class="flex">
              <%= inline_svg_tag('svg/arrow_right.svg', class:'w-4') %>
              <span><%= payment.participant.name %></span>
            </div>
            <span class="text-xs text-placeholder_gray"><%= payment.cadena.name %></span>
          </div>
          <div class="flex flex-col text-right">
            <span class="mb-1"><%= number_to_currency(payment.amount, precision: 0, unit:'$') %></span>
            <span class="text-xs font-light text-placeholder_gray"><%= payment.paid_at.strftime('%d/%m/%Y') %></span>
          </div>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="flex flex-col items-center p-6 border border-gray-700 border-dashed rounded-xl">
      <p class="my-6 text-sm font-medium text-gray-600">No hiciste ningun pago</p>
    </div>
  <% end %>
</div>
