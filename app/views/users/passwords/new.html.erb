<div class="flex justify-center">
  <div class="text-white w-80 font-manrope">
    <h2 class="mb-6 text-3xl font-bold">Problemas de conexion</h2>
    <div class="mb-10">
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "users/shared/error_messages", resource: resource %>
        <div class="mb-4 field">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'input w-full', placeholder:'E-mail' %>
        </div>
        <div class="actions">
          <%= f.submit "Mandar instrucciones", class:'btn-primary' %>
        </div>
      <% end %>
    </div>
    <div class="flex flex-col items-center space-y-4 text-sm">
      <%= link_to "Conectarse", new_session_path(resource_name), class:'text-primary_blue hover:underline' %>
      <span class="w-16 border-t border-black_coral"></span>
      <%= link_to "Inscripción", new_registration_path(resource_name), class:'text-silver hover:underline' %>
    </div>
  </div>
</div>
