<div class="flex flex-col mt-5 space-y-5">
  <%= link_to t('invitation.accept'), accept_cadena_invitation_path(token: params[:token], cadena_id: cadena.id, id: cadena.id), class: 'text-white btn-primary w-full text-center' %>
  <%= link_to t('invitation.decline'), root_url, class: 'btn-danger w-full text-center' %>
</div>
