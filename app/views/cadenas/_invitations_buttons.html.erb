<div class="flex flex-col mt-5 space-y-5">
  <%= link_to t('invitation.accept'), new_user_registration_path(params: { token: params[:token] }), class: 'text-white btn-primary w-full text-center' %>
  <%= link_to t('invitation.decline'), root_url, class: 'btn-danger w-full text-center' %>
</div>
