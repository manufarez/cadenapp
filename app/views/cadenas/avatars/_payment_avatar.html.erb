<%= link_to_cond(current_user&.is_admin?, payment.user == @user ? payment.participant.user : payment.user) do %>
  <%= image_tag 'default_avatar.png', data:{src: payment.user == @user ? payment.participant.avatar.url : payment.user.avatar.url}, class:'lazy w-8 h-8 rounded-full hover:opacity-30 mr-2', alt:'User avatar' %>
<% end %>
